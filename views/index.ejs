<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.0/normalize.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.css">
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Budgtr</title>
</head>
<body>
    <div class="container">
        <h1>Budgtr ALL</h1>
        <nav>
            <a href="/budget/new"><button>Create a New Item</button></a>
        </nav>
        <% let bankAccount = 0
        for (let i = 0; i < allBudget.length; i++)
        {bankAccount += parseInt(allBudget[i].amount)}%> 
        
        <h4 id="total">Bank Account: <%=bankAccount%> </h4>

        <script>
            const bankTotal = document.getElementById("total")
            if (<%= bankAccount%> < 0){
                bankTotal.style.backgroundColor = "red"
            }else if (<%= bankAccount%> > 1000){
                bankTotal.style.backgroundColor = "green"
            }
        </script>

        <table><% for(let i = 0; i < allBudget.length; i++) { %>
            <tr>
                <td>
                    <%=allBudget[i].date %>
                </td>
                <td>
                    <a href="/budget/<%=i%>"><%=allBudget[i].name %></a>
                </td>
                <td>
                    <%=allBudget[i].amount %>
                </td>
            </tr>
        <% } %></table>

    </div>
</body>
</html>

